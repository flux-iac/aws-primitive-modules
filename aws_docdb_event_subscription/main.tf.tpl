######################################################################
# Generated by Tofu-Jet. Please do not manually edit.
# AWS Primitive Module for TF-controller
# Resource: aws_docdb_event_subscription
######################################################################

terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "4.38.0"
    }
  }
}

provider "aws" {
}

variable "values" {
  type = any object({
    enabled = optional(bool)
    event_categories = optional(set(string))
    name = optional(string)
    name_prefix = optional(string)
    sns_topic_arn = optional(string)
    source_ids = optional(set(string))
    source_type = optional(string)
    tags = optional(map(string))
  })
}

resource "aws_docdb_event_subscription" "this" {

  {{- if $.Values.enabled }}
  enabled = var.values.enabled
  {{- end }}
  {{- if $.Values.event_categories }}
  event_categories = var.values.event_categories
  {{- end }}
  {{- if $.Values.name }}
  name = var.values.name
  {{- end }}
  {{- if $.Values.name_prefix }}
  name_prefix = var.values.name_prefix
  {{- end }}
  {{- if $.Values.sns_topic_arn }}
  sns_topic_arn = var.values.sns_topic_arn
  {{- end }}
  {{- if $.Values.source_ids }}
  source_ids = var.values.source_ids
  {{- end }}
  {{- if $.Values.source_type }}
  source_type = var.values.source_type
  {{- end }}
  {{- if $.Values.tags }}
  tags = var.values.tags
  {{- end }}


  {{- if $.Values.count }}
  count = {{ $.Values.count }}
  {{- end }}

  {{- if $.Values.lifecycle }}
  lifecycle {
    {{- if $.Values.lifecycle.create_before_destroy }}
    create_before_destroy = {{ $.Values.lifecycle.create_before_destroy }}
    {{- end }}
    {{- if $.Values.lifecycle.ignore_changes }}
    prevent_destroy = {{ $.Values.lifecycle.prevent_destroy }}
    {{- end }}
    {{- if $.Values.lifecycle.ignore_changes }}
    ignore_changes = {{ $.Values.lifecycle.ignore_changes }}
    {{- end }}
  }
  {{- end }}
}


output "arn" {
    value = aws_docdb_event_subscription.this.arn
}

output "customer_aws_id" {
    value = aws_docdb_event_subscription.this.customer_aws_id
}

output "name" {
    value = aws_docdb_event_subscription.this.name
}

output "sns_topic_arn" {
    value = aws_docdb_event_subscription.this.sns_topic_arn
}

