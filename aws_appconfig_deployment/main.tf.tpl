######################################################################
# Generated by Tofu-Jet. Please do not manually edit.
# AWS Primitive Module for TF-controller
# Resource: aws_appconfig_deployment
######################################################################

terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "4.38.0"
    }
  }
}

provider "aws" {
}

variable "values" {
  type = any object({
    application_id = optional(string)
    configuration_profile_id = optional(string)
    configuration_version = optional(string)
    deployment_strategy_id = optional(string)
    description = optional(string)
    environment_id = optional(string)
    tags = optional(map(string))
  })
}

resource "aws_appconfig_deployment" "this" {

  {{- if $.Values.application_id }}
  application_id = var.values.application_id
  {{- end }}
  {{- if $.Values.configuration_profile_id }}
  configuration_profile_id = var.values.configuration_profile_id
  {{- end }}
  {{- if $.Values.configuration_version }}
  configuration_version = var.values.configuration_version
  {{- end }}
  {{- if $.Values.deployment_strategy_id }}
  deployment_strategy_id = var.values.deployment_strategy_id
  {{- end }}
  {{- if $.Values.description }}
  description = var.values.description
  {{- end }}
  {{- if $.Values.environment_id }}
  environment_id = var.values.environment_id
  {{- end }}
  {{- if $.Values.tags }}
  tags = var.values.tags
  {{- end }}


  {{- if $.Values.count }}
  count = {{ $.Values.count }}
  {{- end }}

  {{- if $.Values.lifecycle }}
  lifecycle {
    {{- if $.Values.lifecycle.create_before_destroy }}
    create_before_destroy = {{ $.Values.lifecycle.create_before_destroy }}
    {{- end }}
    {{- if $.Values.lifecycle.ignore_changes }}
    prevent_destroy = {{ $.Values.lifecycle.prevent_destroy }}
    {{- end }}
    {{- if $.Values.lifecycle.ignore_changes }}
    ignore_changes = {{ $.Values.lifecycle.ignore_changes }}
    {{- end }}
  }
  {{- end }}
}


output "arn" {
    value = aws_appconfig_deployment.this.arn
}

output "deployment_number" {
    value = aws_appconfig_deployment.this.deployment_number
}

output "state" {
    value = aws_appconfig_deployment.this.state
}

