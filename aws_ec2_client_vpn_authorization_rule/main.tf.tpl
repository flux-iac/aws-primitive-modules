######################################################################
# Generated by Tofu-Jet. Please do not manually edit.
# AWS Primitive Module for TF-controller
# Resource: aws_ec2_client_vpn_authorization_rule
######################################################################

terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "4.38.0"
    }
  }
}

provider "aws" {
}

variable "values" {
  type = object({
    access_group_id = optional(string)
    authorize_all_groups = optional(bool)
    client_vpn_endpoint_id = optional(string)
    description = optional(string)
    target_network_cidr = optional(string)
  })
}

resource "aws_ec2_client_vpn_authorization_rule" "this" {

  {{- if $.Values.access_group_id }}
  access_group_id = var.values.access_group_id
  {{- end }}
  {{- if $.Values.authorize_all_groups }}
  authorize_all_groups = var.values.authorize_all_groups
  {{- end }}
  {{- if $.Values.client_vpn_endpoint_id }}
  client_vpn_endpoint_id = var.values.client_vpn_endpoint_id
  {{- end }}
  {{- if $.Values.description }}
  description = var.values.description
  {{- end }}
  {{- if $.Values.target_network_cidr }}
  target_network_cidr = var.values.target_network_cidr
  {{- end }}


  {{- if $.Values.count }}
  count = {{ $.Values.count }}
  {{- end }}

  {{- if $.Values.lifecycle }}
  lifecycle {
    {{- if $.Values.lifecycle.create_before_destroy }}
    create_before_destroy = {{ $.Values.lifecycle.create_before_destroy }}
    {{- end }}
    {{- if $.Values.lifecycle.ignore_changes }}
    prevent_destroy = {{ $.Values.lifecycle.prevent_destroy }}
    {{- end }}
    {{- if $.Values.lifecycle.ignore_changes }}
    ignore_changes = {{ $.Values.lifecycle.ignore_changes }}
    {{- end }}
  }
  {{- end }}
}


