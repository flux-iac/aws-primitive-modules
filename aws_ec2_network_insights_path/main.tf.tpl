######################################################################
# Generated by Tofu-Jet. Please do not manually edit.
# AWS Primitive Module for TF-controller
# Resource: aws_ec2_network_insights_path
######################################################################

terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "4.38.0"
    }
  }
}

provider "aws" {
}

variable "values" {
  type = object({
    destination = optional(string)
    destination_ip = optional(string)
    destination_port = optional(number)
    protocol = optional(string)
    source = optional(string)
    source_ip = optional(string)
    tags = optional(map(string))
  })
}

resource "aws_ec2_network_insights_path" "this" {

  {{- if $.Values.destination }}
  destination = var.values.destination
  {{- end }}
  {{- if $.Values.destination_ip }}
  destination_ip = var.values.destination_ip
  {{- end }}
  {{- if $.Values.destination_port }}
  destination_port = var.values.destination_port
  {{- end }}
  {{- if $.Values.protocol }}
  protocol = var.values.protocol
  {{- end }}
  {{- if $.Values.source }}
  source = var.values.source
  {{- end }}
  {{- if $.Values.source_ip }}
  source_ip = var.values.source_ip
  {{- end }}
  {{- if $.Values.tags }}
  tags = var.values.tags
  {{- end }}


  {{- if $.Values.count }}
  count = {{ $.Values.count }}
  {{- end }}

  {{- if $.Values.lifecycle }}
  lifecycle {
    {{- if $.Values.lifecycle.create_before_destroy }}
    create_before_destroy = {{ $.Values.lifecycle.create_before_destroy }}
    {{- end }}
    {{- if $.Values.lifecycle.ignore_changes }}
    prevent_destroy = {{ $.Values.lifecycle.prevent_destroy }}
    {{- end }}
    {{- if $.Values.lifecycle.ignore_changes }}
    ignore_changes = {{ $.Values.lifecycle.ignore_changes }}
    {{- end }}
  }
  {{- end }}
}


output "arn" {
    value = aws_ec2_network_insights_path.this.arn
}

