######################################################################
# Generated by Tofu-Jet. Please do not manually edit.
# AWS Primitive Module for TF-controller
# Resource: aws_load_balancer_listener_policy
######################################################################

terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "4.38.0"
    }
  }
}

provider "aws" {
}

variable "values" {
  type = any object({
    load_balancer_name = optional(string)
    load_balancer_port = optional(number)
    policy_names = optional(set(string))
  })
}

resource "aws_load_balancer_listener_policy" "this" {

  {{- if $.Values.load_balancer_name }}
  load_balancer_name = var.values.load_balancer_name
  {{- end }}
  {{- if $.Values.load_balancer_port }}
  load_balancer_port = var.values.load_balancer_port
  {{- end }}
  {{- if $.Values.policy_names }}
  policy_names = var.values.policy_names
  {{- end }}


  {{- if $.Values.count }}
  count = {{ $.Values.count }}
  {{- end }}

  {{- if $.Values.lifecycle }}
  lifecycle {
    {{- if $.Values.lifecycle.create_before_destroy }}
    create_before_destroy = {{ $.Values.lifecycle.create_before_destroy }}
    {{- end }}
    {{- if $.Values.lifecycle.ignore_changes }}
    prevent_destroy = {{ $.Values.lifecycle.prevent_destroy }}
    {{- end }}
    {{- if $.Values.lifecycle.ignore_changes }}
    ignore_changes = {{ $.Values.lifecycle.ignore_changes }}
    {{- end }}
  }
  {{- end }}
}


