######################################################################
# Generated by Tofu-Jet. Please do not manually edit.
# AWS Primitive Module for TF-controller
# Resource: aws_ec2_local_gateway_route
######################################################################

terraform {
  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "4.38.0"
    }
  }
}

provider "aws" {
}

variable "values" {
  type = object({
    destination_cidr_block = optional(string)
    local_gateway_route_table_id = optional(string)
    local_gateway_virtual_interface_group_id = optional(string)
  })
}

resource "aws_ec2_local_gateway_route" "this" {

  {{- if $.Values.destination_cidr_block }}
  destination_cidr_block = var.values.destination_cidr_block
  {{- end }}
  {{- if $.Values.local_gateway_route_table_id }}
  local_gateway_route_table_id = var.values.local_gateway_route_table_id
  {{- end }}
  {{- if $.Values.local_gateway_virtual_interface_group_id }}
  local_gateway_virtual_interface_group_id = var.values.local_gateway_virtual_interface_group_id
  {{- end }}


  {{- if $.Values.count }}
  count = {{ $.Values.count }}
  {{- end }}

  {{- if $.Values.lifecycle }}
  lifecycle {
    {{- if $.Values.lifecycle.create_before_destroy }}
    create_before_destroy = {{ $.Values.lifecycle.create_before_destroy }}
    {{- end }}
    {{- if $.Values.lifecycle.ignore_changes }}
    prevent_destroy = {{ $.Values.lifecycle.prevent_destroy }}
    {{- end }}
    {{- if $.Values.lifecycle.ignore_changes }}
    ignore_changes = {{ $.Values.lifecycle.ignore_changes }}
    {{- end }}
  }
  {{- end }}
}


